server:
  shutdown: graceful

spring:
  application:
    name: mopl-api
  profiles:
    active: local
  config:
    import:
      - optional:file:.env[.properties]
  threads:
    virtual:
      enabled: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
